RewriteEngine On

# If file or folder exists → load it normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise → send to React
RewriteRule ^ index.html [L]
